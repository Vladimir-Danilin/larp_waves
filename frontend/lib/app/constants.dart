import 'package:flutter/material.dart';


final Color backgroundLightColor = Color(0xFFF2F2F2);

final Color surfaceLightColor = Color(0xFFFFFFFF);

final Color testColor = Color(0xFF3A827B);

final ColorScheme lightColorSchema = ColorScheme(
    brightness: Brightness.light,
    primary: testColor,
    onPrimary: testColor,
    secondary: testColor,
    onSecondary: testColor,
    error: testColor,
    onError: testColor,
    surface: testColor,
    onSurface: testColor
);
